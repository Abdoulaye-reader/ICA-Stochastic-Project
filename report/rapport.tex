\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{geometry}
\usepackage{graphicx}
\geometry{margin=2.5cm}

\title{Implementation et Evaluation de Methodes ICA Classiques et Stochastiques}
\author{Abdoulaye Diallo}
\date{2 avril 2026}

\begin{document}
\maketitle
\tableofcontents
\newpage

\section{Introduction}

L'Analyse en Composantes Independantes (ICA) cherche a retrouver des sources latentes statistiquement independantes a partir de signaux observes qui sont des melanges lineaires. Ce cadre est central en separation aveugle de sources, notamment en audio, en biomedical et en traitement de signaux multicanaux.

Dans ce projet, l'objectif principal est de comparer trois approches sous un meme cadre experimental: FastICA comme methode de reference, puis deux variantes stochastiques basees sur le critere Infomax, a savoir SGD-ICA et Adam-ICA. La qualite de separation est evaluee principalement avec l'indice d'Amari \cite{amari1996new}, et l'analyse est completee par une etude de convergence sur la fonction objective et une etude de robustesse au bruit.

La contribution creative du travail est consacree au role de la gaussianite des sources. L'idee est de sortir d'une comparaison purement algorithmique pour interroger une hypothese structurante de l'ICA: la non-gaussianite. Cette partie est redigee en fin de rapport comme une proposition methodologique pleinement exploitable pour une extension experimentale.

\section{Fondements theoriques}

\subsection{Modele ICA}

Le modele lineaire considere est:
\begin{equation}
x = A s,
\end{equation}
ou $s \in \mathbb{R}^D$ designe le vecteur des sources independantes, $A \in \mathbb{R}^{D \times D}$ la matrice de melange, et $x$ les observations. Le probleme consiste a estimer une matrice de demelange $V$ telle que $y = Vx$ reproduise les sources a permutation et echelle pres.

\subsection{Non-gaussianite et identifiabilite}

L'ICA repose sur un resultat classique: l'identification est possible si les sources sont independantes et si au plus une source est gaussienne. En pratique, cette hypothese est capturee par des non-linearites adaptees qui favorisent les distributions super-gaussiennes ou sub-gaussiennes, selon le contexte.

\subsection{Critere Infomax}

Pour une observation $x$ et $y=Vx$, la log-vraisemblance echantillon s'ecrit:
\begin{equation}
\ell(V;x)=\log|\det(V)|+\sum_{j=1}^{D}\log p_j(y_j).
\end{equation}

En posant la fonction score $g(u)=\frac{d}{du}\log p(u)$, on obtient le gradient:
\begin{equation}
\nabla_V\ell(V;x)=V^{-T}+g(y)x^T.
\end{equation}

Apres centrage et whitening, on impose numeriquement $VV^T \approx I$, ce qui conduit a l'approximation $V^{-T}\approx V$ et a la mise a jour:
\begin{equation}
V \leftarrow V + \eta\left(V+\mathbb{E}[g(Vx)x^T]\right).
\end{equation}

C'est cette forme qui est utilisee dans les variantes stochastiques implementees.

\section{Methodes et protocole experimental}

\subsection{Algorithmes implementes}

FastICA est utilise via \texttt{sklearn.decomposition.FastICA} (mode parallel, non-linearite \texttt{logcosh}), avec pretraitement identique aux methodes stochastiques pour garantir une comparaison propre.

SGD-ICA et Adam-ICA partagent le meme gradient Infomax et la meme fonction score, avec $g(u)=-\tanh(u)$, conformement a une prior de type \texttt{logcosh}. La difference est strictement dans la dynamique d'optimisation: pas constant pour SGD, pas adaptatif pour Adam. Dans les deux cas, une re-orthogonalisation QR est appliquee a chaque iteration pour maintenir la contrainte numerique induite par le whitening.

\subsection{Donnees et metrique}

Les experiences sont menees sur des donnees synthetiques generees selon le modele ICA, avec repetitions sur plusieurs graines aleatoires pour estimer la stabilite des conclusions.

La metrique de qualite principale est l'indice d'Amari, calcule a partir de $C=\hat V A$:
\begin{equation}
r(C)=\frac{1}{2D(D-1)}\left[\sum_i\left(\frac{\sum_j|c_{ij}|}{\max_k|c_{ik}|}-1\right)+\sum_j\left(\frac{\sum_i|c_{ij}|}{\max_k|c_{kj}|}-1\right)\right].
\end{equation}

Une valeur proche de 0 signifie une separation de bonne qualite.

\section{Resultats experimentaux}

\subsection{Scalabilite}

La premiere campagne etudie la scalabilite, d'abord en dimension ($D$ variable, $N$ fixe), puis en nombre d'echantillons ($N$ variable, $D$ fixe). Les tendances observees sont stables: FastICA conserve un indice d'Amari faible sur toute la plage de test, alors que SGD-ICA et Adam-ICA restent plus sensibles aux reglages et se stabilisent sur un niveau de separation moins precis.

\begin{table}[h]
\centering
\begin{tabular}{lcc}
\toprule
Algorithme & Amari moyen (D=3 $\rightarrow$ 40) & Temps moyen (D=3 $\rightarrow$ 40) \\
\midrule
FastICA & 0.0124 $\rightarrow$ 0.0129 & 0.014s $\rightarrow$ 0.584s \\
SGD-ICA & 0.1516 $\rightarrow$ 0.3044 & 0.521s $\rightarrow$ 14.315s \\
Adam-ICA & 0.3073 $\rightarrow$ 0.3116 & 0.488s $\rightarrow$ 12.151s \\
\bottomrule
\end{tabular}
\caption{Tendance de scalabilite en dimension ($N=5000$, moyenne sur 3 seeds).}
\end{table}

\begin{table}[h]
\centering
\begin{tabular}{lcc}
\toprule
Algorithme & Amari moyen (N=1000 $\rightarrow$ 10000) & Temps moyen (N=1000 $\rightarrow$ 10000) \\
\midrule
FastICA & 0.0289 $\rightarrow$ 0.0089 & 0.067s $\rightarrow$ 0.470s \\
SGD-ICA & 0.3451 $\rightarrow$ 0.3206 & 0.085s $\rightarrow$ 9.542s \\
Adam-ICA & 0.3474 $\rightarrow$ 0.3294 & 0.067s $\rightarrow$ 8.646s \\
\bottomrule
\end{tabular}
\caption{Tendance de scalabilite en nombre d'echantillons ($D=20$, budget stochastique en epochs fixes).}
\end{table}

\subsection{Convergence des methodes stochastiques}

La deuxieme campagne isole la dynamique de convergence de SGD-ICA et Adam-ICA via la fonction objective Infomax. Ici, l'objectif n'est plus seulement la qualite finale, mais la trajectoire d'optimisation iteration par iteration.

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
Algorithme & Objective initiale & Objective finale & Gain & Iterations \\
\midrule
SGD-ICA & -0.3637 & -0.3504 & +0.0133 & 2000 \\
Adam-ICA & -0.3637 & -0.3608 & +0.0030 & 2000 \\
\bottomrule
\end{tabular}
\caption{Convergence de la fonction objective (moyennes sur 3 seeds).}
\end{table}

On observe une progression plus marquee pour SGD-ICA avec les reglages utilises, alors qu'Adam-ICA evolue plus lentement et atteint un plateau plus conservateur. Ce comportement est coherent avec les pas d'apprentissage retenus et avec la variabilite introduite par l'echantillonnage mini-batch.

\begin{figure}[h]
\centering
\includegraphics[width=0.82\textwidth]{../results/convergence_plot.png}
\caption{Evolution de la fonction objective au cours des iterations pour SGD-ICA et Adam-ICA.}
\label{fig:convergence}
\end{figure}

\subsection{Robustesse au bruit}

La troisieme campagne ajoute un bruit gaussien aux observations pour comparer la degradation de performance des trois algorithmes en fonction du niveau de bruit.

\begin{table}[h]
\centering
\begin{tabular}{lccc}
\toprule
Niveau de bruit $\sigma$ & FastICA & SGD-ICA & Adam-ICA \\
\midrule
0.0 & 0.0145 & 0.2389 & 0.3498 \\
0.1 & 0.0262 & 0.2700 & 0.3704 \\
0.2 & 0.0392 & 0.2625 & 0.3738 \\
0.5 & 0.0590 & 0.3289 & 0.3477 \\
\bottomrule
\end{tabular}
\caption{Indice d'Amari moyen selon le niveau de bruit (3 seeds).}
\end{table}

FastICA reste la methode la plus precise sur l'ensemble des niveaux testes. Les deux variantes stochastiques se degradent davantage, avec un avantage modere pour SGD-ICA face a Adam-ICA lorsque le bruit devient fort.

\begin{figure}[h]
\centering
\includegraphics[width=0.82\textwidth]{../results/noise_robustness_plot.png}
\caption{Comparaison sous bruit gaussien: Amari (gauche) et temps de calcul (droite).}
\label{fig:noise_robustness}
\end{figure}

\section{Contribution creative: role de la gaussianite des sources}

La partie creative du projet porte sur une question de fond: comment la proximite a la gaussianite modifie-t-elle la difficulte du probleme ICA, a algorithme fixe? Cette question est importante, car l'hypothese de non-gaussianite n'est pas un detail technique mais la condition meme d'identifiabilite.

\subsection{Idee methodologique}

Pour parametrer finement la forme des sources, on propose d'utiliser une famille de lois gaussiennes generalisees de densite
\begin{equation}
p_\beta(s) \propto \exp\left(-\left|\frac{s}{\alpha}\right|^\beta\right),
\end{equation}
ou $\beta$ controle la forme de la distribution: $\beta<2$ produit des queues plus lourdes (super-gaussien), $\beta=2$ redonne la gaussienne, et $\beta>2$ donne une distribution plus plate (sub-gaussien).

L'experience consiste ensuite a faire varier $\beta$ sur une grille, a regenerer les melanges pour chaque valeur, puis a mesurer Amari et dynamique de convergence pour FastICA, SGD-ICA et Adam-ICA dans des conditions de calcul comparables.

\subsection{Hypothese scientifique et interpretation attendue}

L'hypothese centrale est la suivante: plus les sources se rapprochent d'une gaussienne, plus les contrastes non lineaires deviennent moins informatifs, et plus la separation se degrade. On s'attend donc a observer un minimum de performance autour de $\beta \approx 2$, avec de meilleures recuperations dans les regimes nettement super-gaussiens ou sub-gaussiens.

Cette contribution apporte une lecture mecaniste des resultats observes dans le reste du rapport. Elle ne se limite pas a comparer des scores finaux: elle relie explicitement la performance algorithmique a la structure statistique des sources. En ce sens, elle prolonge le projet vers une analyse plus explicative, et non seulement descriptive.

\section{Discussion generale}

Les resultats obtenus montrent une hierarchie claire dans ce cadre de travail: FastICA domine en precision de separation et en cout de calcul sur donnees synthetiques, tandis que les variantes stochastiques sont plus sensibles aux hyperparametres. Toutefois, l'analyse de convergence indique que SGD-ICA peut progresser de maniere reguliere et exploiter efficacement le cadre mini-batch lorsque les reglages sont favorables.

L'etude de robustesse au bruit confirme cette lecture: les methodes stochastiques restent fonctionnelles mais se degradent plus vite, alors que FastICA conserve une bonne stabilite. Ces constats ne ferment pas la porte aux approches stochastiques; ils indiquent plutot qu'une comparaison stricte a budget temps egal et un tuning plus fin (en particulier pour Adam) sont necessaires pour juger leur avantage en grande echelle.

Enfin, la contribution creative sur la gaussianite clarifie le point theorique clef du projet: la qualite de separation depend autant du choix d'optimisation que de la nature statistique des sources. Cette articulation entre theorie, implementation et analyse empirique constitue l'apport principal du rapport.

\section{Conclusion}

Ce travail a permis d'implementer et d'evaluer FastICA, SGD-ICA et Adam-ICA dans un cadre experimental coherent, avec des mesures conjointes de qualite (Amari), de convergence et de robustesse au bruit. Les campagnes menees montrent que FastICA reste la reference la plus fiable dans le regime synthetique teste, tandis que SGD-ICA presente une dynamique de convergence prometteuse parmi les approches stochastiques.

La suite naturelle est de finaliser experimentalement l'etude de gaussianite par balayage du parametre de forme $\beta$, puis d'etendre la validation a des donnees reelles. Cette extension permettrait de transformer la contribution creative en resultat empirique complet, tout en conservant la ligne directrice scientifique du projet.

\bibliographystyle{plain}
\bibliography{references}

\end{document}
